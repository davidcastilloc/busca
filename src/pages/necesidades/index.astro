---
export const prerender = false;

const id = Astro.url.searchParams.get("id");
if (id) {
  return Astro.redirect(`/mapa?tipo=necesidad&id=${id}`);
}
return Astro.redirect("/mapa?layer=necesidades");
---
