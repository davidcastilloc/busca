---
---

<div class="w-full max-w-3xl mx-auto px-2 py-2 sm:py-4">
  <div class="hidden flex gap-2 sm:gap-3 justify-center mb-4 sm:mb-6">
    <button id="btn-tab-exact" class="flex-1 sm:flex-none px-4 sm:px-5 py-3 rounded-[36px] bg-primary text-white font-uber-text uber-body-md-strong transition-colors cursor-pointer text-center">
      🔍 Por Nombre / Cédula
    </button>
    <button id="btn-tab-semantic" class="hidden flex-1 sm:flex-none px-4 sm:px-5 py-3 rounded-[36px] bg-canvas-soft text-ink hover:bg-surface-pressed font-uber-text uber-body-md-strong transition-colors cursor-pointer text-center">
      🧠 Descripción / IA
    </button>
  </div>

  <!-- Formulario 1: Búsqueda Exacta -->
  <div id="panel-exact" class="bg-canvas border border-canvas-soft p-4 sm:p-8 rounded-xl shadow-sm">
    <form id="form-search-exact" class="space-y-4" onsubmit="event.preventDefault();">
      <div class="flex flex-col sm:flex-row items-stretch gap-2.5 relative bg-canvas">
        <div class="relative flex-grow">
          <input 
            type="text" 
            id="query-exact" 
            required
            autocomplete="off"
            class="w-full font-uber-text uber-body-md bg-canvas-soft border border-transparent text-ink placeholder:text-mute focus:border-ink focus:bg-canvas transition-all duration-200 py-3.5 px-4 outline-none rounded-none" 
            placeholder="Nombre, Cédula, Ubicación o Refugio..." 
          />
          <div id="search-suggestions" class="hidden absolute top-full left-0 right-0 z-50 mt-1 bg-canvas border border-canvas-soft rounded-none shadow-lg max-h-60 overflow-y-auto divide-y divide-canvas-soft"></div>
        </div>
        <button type="submit" class="bg-primary text-white hover:bg-black-elevated font-uber-text uber-body-md-strong px-8 py-3.5 rounded-full transition-transform active:scale-[0.98] flex items-center justify-center shrink-0 cursor-pointer">
          Buscar
        </button>
      </div>

      <!-- Escenarios rápidos -->
      <div class="hidden space-y-2">
        <p class="text-[11px] text-mute font-uber-text font-medium uppercase tracking-wider">¿Qué dato tienes?</p>
        <div class="flex flex-wrap gap-2">
          <button type="button" class="chip-scenario px-3 py-1.5 rounded-full bg-canvas-soft text-ink text-xs font-uber-text font-medium border border-canvas-soft hover:bg-surface-pressed transition-colors cursor-pointer" data-placeholder="Ej: María García" data-tab="exact">
            👤 Sé el nombre
          </button>
          <button type="button" class="chip-scenario px-3 py-1.5 rounded-full bg-canvas-soft text-ink text-xs font-uber-text font-medium border border-canvas-soft hover:bg-surface-pressed transition-colors cursor-pointer" data-placeholder="Ej: 12345678" data-tab="exact">
            🪪 Tengo la cédula
          </button>
          <button type="button" class="chip-scenario px-3 py-1.5 rounded-full bg-canvas-soft text-ink text-xs font-uber-text font-medium border border-canvas-soft hover:bg-surface-pressed transition-colors cursor-pointer" data-placeholder="Ej: Albergue Polideportivo" data-tab="exact">
            📍 Sé dónde estaba
          </button>
          <button type="button" class="hidden chip-scenario px-3 py-1.5 rounded-full bg-yellow-500/10 text-yellow-800 text-xs font-uber-text font-medium border border-yellow-500/30 hover:bg-yellow-500/20 transition-colors cursor-pointer" data-tab="semantic">
            ❓ No sé el nombre
          </button>
        </div>
      </div>

      <p class="text-xs text-body font-uber-text">
        Búsqueda en el registro colaborativo aportado por voluntarios y rescatistas.
      </p>
    </form>
  </div>

  <!-- Formulario 2: Búsqueda Semántica (IA) -->
  <div id="panel-semantic" class="bg-canvas border border-canvas-soft p-4 sm:p-8 rounded-xl shadow-sm hidden">
    <form id="form-search-semantic" class="space-y-4" onsubmit="event.preventDefault();">
      <!-- Banner informativo -->
      <div class="flex items-start gap-3 p-3 bg-blue-500/5 border border-blue-500/20 rounded-xl">
        <span class="text-lg mt-0.5">🧠</span>
        <div class="text-xs text-body font-uber-text space-y-1">
          <p class="font-semibold text-ink">¿No sabes el nombre? Describe lo que recuerdas.</p>
          <p>La IA busca reportes con características similares: ropa, edad, zona, rasgos físicos.</p>
        </div>
      </div>

      <div class="space-y-2">
        <label class="block font-uber-text uber-body-md-strong text-ink">Describe a la persona o situación</label>
        <textarea 
          id="query-semantic" 
          required
          minlength="5"
          rows="4"
          class="textarea w-full font-uber-text uber-body-md" 
          placeholder="Ej: Señora mayor, cabello blanco, vestido floreado, zona de Petare..."
        ></textarea>
      </div>

      <!-- Escenarios rápidos para IA -->
      <div class="space-y-2">
        <p class="text-[11px] text-mute font-uber-text font-medium uppercase tracking-wider">Escenarios comunes — toca para usar de plantilla</p>
        <div class="flex flex-wrap gap-2">
          <button type="button" class="chip-fill px-3 py-1.5 rounded-full bg-canvas-soft text-ink text-xs font-uber-text font-medium border border-canvas-soft hover:bg-surface-pressed transition-colors cursor-pointer" data-fill="Niño/niña de aproximadamente _ años, visto con _, visto/a por última vez en _">
            👶 Niño/a perdido
          </button>
          <button type="button" class="chip-fill px-3 py-1.5 rounded-full bg-canvas-soft text-ink text-xs font-uber-text font-medium border border-canvas-soft hover:bg-surface-pressed transition-colors cursor-pointer" data-fill="Persona mayor de edad avanzada, cabello _, complexión _, vista por última vez en la zona de _">
            👴 Adulto mayor
          </button>
          <button type="button" class="chip-fill px-3 py-1.5 rounded-full bg-canvas-soft text-ink text-xs font-uber-text font-medium border border-canvas-soft hover:bg-surface-pressed transition-colors cursor-pointer" data-fill="Persona herida encontrada en _, con características: _, necesita atención médica">
            🏥 Vi a alguien herido
          </button>
          <button type="button" class="chip-fill px-3 py-1.5 rounded-full bg-canvas-soft text-ink text-xs font-uber-text font-medium border border-canvas-soft hover:bg-surface-pressed transition-colors cursor-pointer" data-fill="Persona atrapada o en situación de riesgo en _, descripción: _, zona afectada por _">
            🆘 Alguien atrapado
          </button>
          <button type="button" class="chip-fill px-3 py-1.5 rounded-full bg-canvas-soft text-ink text-xs font-uber-text font-medium border border-canvas-soft hover:bg-surface-pressed transition-colors cursor-pointer" data-fill="Grupo de personas desplazadas en la zona de _, aproximadamente _ personas, incluyen niños/adultos mayores, necesitan _">
            👥 Grupo desplazado
          </button>
        </div>
      </div>

      <button type="submit" id="btn-submit-semantic" class="bg-primary text-white hover:bg-black-elevated w-full font-uber-text uber-body-md-strong py-3.5 rounded-full transition-transform active:scale-[0.98] flex items-center justify-center cursor-pointer">
        Buscar por similitud IA
      </button>
      <p class="text-xs text-body font-uber-text">
        Procesa la descripción y busca reportes con características similares. Requiere conexión.
      </p>
    </form>
  </div>

  <!-- Filtros de estado sub-tabs (inicialmente oculto) -->
  <div id="filter-state-bar" class="hidden flex gap-2 justify-start mt-6 mb-2 border-b border-canvas-soft pb-3">
    <button id="btn-filter-all" class="px-4 py-1.5 rounded-full bg-primary text-white font-uber-text text-xs font-semibold transition-colors cursor-pointer">
      Todos
    </button>
    <button id="btn-filter-nocontact" class="px-4 py-1.5 rounded-full bg-canvas-soft text-ink hover:bg-surface-pressed font-uber-text text-xs font-semibold transition-colors cursor-pointer">
      Sin contacto
    </button>
    <button id="btn-filter-located" class="px-4 py-1.5 rounded-full bg-canvas-soft text-ink hover:bg-surface-pressed font-uber-text text-xs font-semibold transition-colors cursor-pointer">
      Localizados
    </button>
  </div>

  <!-- Resultados de búsqueda -->
  <div id="results-loader" class="hidden flex justify-center py-10">
    <span class="loading loading-spinner loading-lg text-primary"></span>
  </div>

  <div id="results-header" class="hidden font-uber-text uber-body-md-strong text-body mt-8 mb-4"></div>
  <div id="results-container" class="space-y-4"></div>
  
  <!-- Sentinel para scroll infinito -->
  <div id="scroll-sentinel" class="hidden justify-center py-6">
    <span class="loading loading-spinner text-primary"></span>
  </div>
</div>

<!-- ═══════════════════════════════════════════════════════
     MODAL FULLSCREEN DE DETALLE
     ═══════════════════════════════════════════════════════ -->
<div id="modal-detalle" class="fixed inset-0 z-[100] bg-black/60 hidden" style="backdrop-filter:blur(4px)">
  <div class="absolute inset-0 sm:inset-4 md:inset-8 bg-canvas rounded-none sm:rounded-2xl overflow-y-auto overscroll-contain flex flex-col">
    <!-- Header fijo -->
    <div class="sticky top-0 z-10 bg-canvas/95 border-b border-canvas-soft px-4 py-3 flex items-center justify-between" style="backdrop-filter:blur(8px)">
      <span id="modal-tipo-badge" class="text-xs font-uber-text font-medium px-3 py-1 rounded-full"></span>
      <button id="modal-close" class="w-10 h-10 rounded-full bg-canvas-soft flex items-center justify-center text-ink hover:bg-surface-pressed transition-colors text-lg cursor-pointer active:bg-surface-pressed">✕</button>
    </div>

    <!-- Contenido scrolleable -->
    <div id="modal-body" class="flex-grow p-4 sm:p-6 pb-32">
      <!-- Se llena dinámicamente -->
    </div>

    <!-- Barra de acciones fija abajo -->
    <div id="modal-actions" class="sticky bottom-0 bg-canvas border-t border-canvas-soft p-4 flex flex-wrap gap-2">
      <!-- Se llena dinámicamente -->
    </div>
  </div>
</div>

<!-- ═══════════════════════════════════════════════════════
     DIV OCULTO PARA GENERAR CARTEL "SE BUSCA"
     ═══════════════════════════════════════════════════════ -->
<div id="cartel-container" style="position:absolute;left:-9999px;top:-9999px;">
  <div id="cartel-se-busca" style="width:600px;font-family:system-ui,-apple-system,sans-serif;background:#fff;color:#000;">
    <!-- Franja superior negra -->
    <div style="background:#000;color:#fff;padding:16px 24px;text-align:center;">
      <div style="font-size:12px;letter-spacing:4px;font-weight:500;margin-bottom:4px;">dondeestan.org</div>
      <div style="font-size:36px;font-weight:700;letter-spacing:2px;">DONDE ESTAN</div>
    </div>
    
    <!-- Foto -->
    <div id="cartel-foto-wrap" style="width:100%;height:400px;background:#efefef;display:flex;align-items:center;justify-content:center;overflow:hidden;position:relative;">
      <img id="cartel-foto" style="position:absolute;" src="" alt="" crossorigin="anonymous" />
    </div>
    
    <!-- Datos -->
    <div style="padding:20px 24px;">
      <div id="cartel-nombre" style="font-size:28px;font-weight:700;line-height:1.2;margin-bottom:8px;"></div>
      <div id="cartel-datos" style="font-size:14px;color:#5e5e5e;margin-bottom:12px;"></div>
      <div id="cartel-descripcion" style="font-size:15px;line-height:1.5;margin-bottom:16px;border-left:3px solid #000;padding-left:12px;font-style:italic;"></div>
      <div id="cartel-ubicacion" style="font-size:14px;color:#5e5e5e;"></div>
    </div>
    
    <!-- Franja inferior negra -->
    <div style="background:#000;color:#fff;padding:14px 24px;display:flex;justify-content:space-between;align-items:center;">
      <div>
        <div id="cartel-contacto" style="font-size:16px;font-weight:700;"></div>
      </div>
      <div style="font-size:11px;opacity:0.7;text-align:right;">
        <div>dondeestan.org</div>
        <div>dondeestan.org</div>
      </div>
    </div>
  </div>
</div>

<!-- Mini formulario de reporte rápido (hidden) -->
<div id="modal-reporte-rapido" class="hidden fixed inset-0 z-[110] bg-black/60 flex items-end sm:items-center justify-center animate-fade-in" style="backdrop-filter:blur(4px)">
  <div class="bg-canvas w-full sm:max-w-md sm:rounded-2xl rounded-t-2xl p-6 space-y-4 max-h-[90vh] overflow-y-auto border border-canvas-soft shadow-xl">
    <h3 class="font-uber-display uber-display-sm text-ink">📢 Reportar Avistamiento</h3>
    <p class="text-xs text-body font-uber-text">Registrar que viste a esta persona. La información se vinculará a su reporte de búsqueda.</p>
    
    <div class="space-y-3">
      <div>
        <label class="block text-[10px] font-bold text-mute uppercase tracking-wider mb-1 font-uber-text" for="reporte-rapido-nombre">Tu Nombre</label>
        <input 
          type="text" 
          id="reporte-rapido-nombre" 
          class="input w-full font-uber-text uber-body-md" 
          placeholder="Opcional (Nombre de quien reporta)"
        />
      </div>
      <div>
        <label class="block text-[10px] font-bold text-mute uppercase tracking-wider mb-1 font-uber-text" for="reporte-rapido-contacto">Tu Teléfono / Contacto *</label>
        <input 
          type="text" 
          id="reporte-rapido-contacto" 
          required 
          class="input w-full font-uber-text uber-body-md" 
          placeholder="Obligatorio (ej: 0412-1234567, email)"
        />
      </div>
      <div>
        <label class="block text-[10px] font-bold text-mute uppercase tracking-wider mb-1 font-uber-text" for="reporte-rapido-desc">¿Dónde y cuándo la viste? Detalles *</label>
        <textarea 
          id="reporte-rapido-desc" 
          rows="3" 
          class="textarea w-full font-uber-text uber-body-md" 
          placeholder="Ej: La vi hoy a las 10am en el refugio del Polideportivo, vestía camisa azul... (mínimo 10 caracteres)"
        ></textarea>
      </div>

      <div>
        <label class="block text-[10px] font-bold text-mute uppercase tracking-wider mb-1 font-uber-text">Foto Evidencia * (Obligatoria)</label>
        <div class="flex items-center gap-3 bg-canvas-soft border border-canvas-soft p-3 rounded-xl">
          <input 
            type="file" 
            id="reporte-rapido-foto-file" 
            accept="image/*" 
            class="block w-full text-xs text-mute font-uber-text file:mr-4 file:py-1 file:px-3 file:rounded-full file:border-0 file:text-xs file:font-semibold file:bg-primary file:text-white hover:file:bg-black-elevated file:cursor-pointer" 
          />
          <input type="hidden" id="reporte-rapido-foto-key" />
        </div>
        <div id="reporte-rapido-foto-status" class="text-[10px] text-mute font-uber-text mt-1 italic hidden"></div>
      </div>

      <!-- GPS Status Panel -->
      <div class="p-3 bg-canvas-soft rounded-xl flex flex-col gap-1 border border-canvas-soft">
        <div class="flex justify-between items-center">
          <span class="text-xs font-semibold text-ink font-uber-text">📍 Ubicación del Avistamiento</span>
          <button id="btn-reporte-rapido-gps" type="button" class="btn btn-xs bg-primary text-white font-uber-text px-2 py-0.5 rounded-full text-[11px] cursor-pointer hover:bg-black-elevated">
            Recargar GPS
          </button>
        </div>
        <div id="reporte-rapido-gps-status" class="text-xs text-mute font-uber-text italic">Iniciando búsqueda...</div>
        <input type="hidden" id="reporte-rapido-latitud" />
        <input type="hidden" id="reporte-rapido-longitud" />
        <div id="reporte-rapido-gps-map" class="w-full h-36 rounded-lg overflow-hidden border border-canvas-soft bg-canvas mt-2 hidden"></div>
      </div>

      <!-- Checkbox de confirmación -->
      <div class="flex items-start gap-2 pt-1">
        <input 
          type="checkbox" 
          id="chk-reporte-rapido-confirmacion" 
          class="w-4 h-4 rounded border-2 border-primary bg-canvas text-primary focus:ring-0 focus:ring-offset-0 mt-0.5 accent-black cursor-pointer" 
          required 
        />
        <label for="chk-reporte-rapido-confirmacion" class="text-xs text-body font-uber-text select-none cursor-pointer">
          Confirmo que esta información es verídica y autorizo que los familiares me contacten si es necesario.
        </label>
      </div>
    </div>

    <!-- Error message display -->
    <div id="reporte-rapido-error" class="hidden text-xs text-red-500 font-uber-text font-semibold italic"></div>

    <div class="flex gap-2">
      <button id="reporte-rapido-enviar" class="btn bg-primary text-white flex-1 font-uber-text uber-body-md-strong rounded-full hover:bg-black-elevated cursor-pointer">Enviar Reporte</button>
      <button id="reporte-rapido-cancelar" class="btn bg-canvas-soft text-ink flex-1 font-uber-text uber-body-md-strong rounded-full hover:bg-surface-pressed cursor-pointer">Cancelar</button>
    </div>
  </div>
</div>

<!-- Modal de Compartir en Redes (Fallback para desktop y acceso rapido) -->
<div id="modal-compartir-redes" class="fixed inset-0 z-[120] bg-black/60 flex items-center justify-center hidden" style="backdrop-filter:blur(4px)">
  <div class="bg-canvas w-full max-w-sm rounded-2xl p-6 space-y-4 border border-canvas-soft shadow-xl mx-4">
    <div class="flex justify-between items-center">
      <h3 class="font-uber-display uber-display-sm text-ink">🔗 Compartir Reporte</h3>
      <button id="modal-compartir-close" class="w-8 h-8 rounded-full bg-canvas-soft flex items-center justify-center text-ink hover:bg-surface-pressed transition-colors font-bold cursor-pointer">✕</button>
    </div>
    <p class="text-xs text-body font-uber-text">Elige dónde quieres difundir la información de búsqueda:</p>
    <div class="grid grid-cols-2 gap-2 pt-2">
      <a id="share-wa" target="_blank" rel="noopener" class="btn bg-[#25D366] text-white hover:opacity-90 font-uber-text text-sm flex items-center justify-center rounded-full py-3">
        WhatsApp
      </a>
      <a id="share-fb" target="_blank" rel="noopener" class="btn bg-[#1877F2] text-white hover:opacity-90 font-uber-text text-sm flex items-center justify-center rounded-full py-3">
        Facebook
      </a>
      <a id="share-x" target="_blank" rel="noopener" class="btn bg-black text-white hover:opacity-90 font-uber-text text-sm flex items-center justify-center rounded-full py-3">
        Twitter / X
      </a>
      <button id="share-copy" class="btn bg-canvas-soft text-ink hover:bg-surface-pressed font-uber-text text-sm flex items-center justify-center rounded-full py-3">
        Copiar enlace
      </button>
    </div>
  </div>
</div>

<script src="../scripts/buscador.ts"></script>
